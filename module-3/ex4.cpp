#include <iostream>

using namespace std;

int main(){
	int a, b;
	cin >> a >> b;
	cout << "quotient = " << a/b << " and remainder = " << a%b << endl;
	/*
		Just to test your knowledge in operators
		Unlike in Python, JavaScript, Haskell etc where you need a//b for int division
		{int}/{int} in C++ is integer division by default.
	*/
	
	return 0;
}
